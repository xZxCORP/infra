[master]
master_01 ansible_host=${master_ip}
[worker]
%{ for index, ip in worker_ips ~}
worker_${format("%02d", index + 1)} ansible_host=${ip}
%{ endfor ~}
[runner]
runner_01 ansible_host=${runner_ip}